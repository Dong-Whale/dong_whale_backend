spring:
    datasource:
        url: jdbc:postgresql://localhost:5433/template1
        username: postgres
        password:
        driverClassName: org.postgresql.Driver
        initialization-mode: always
        initialize: true
        continue-on-error: true
    jpa:
        hibernate :
            dialect : org.hibernate.dialect.PostgreSQLDialect
            ddl-auto : update
        properties:
            hibernate :
                show_sql : true
                format_sql : true

logging:
    level:
        org.hibernate.SQL : debug
        org.hibernate.type : trace

amazon:
    aws:
        bucket-name : bucketeer-7db48d7b-1395-4b1b-8695-5abc6f2e95d1
        region: us-east-1
        access-key : AKIAX7CRDYXP5SOHEMUF
        secret-key : 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
